_site/
.jekyll-cache/
.jekyll-metadata
.sass-cache/
Gemfile.lock
vendor/
.DS_Store

# Los borradores llevan andamiaje con contexto interno (nombres, cifras,
# criterio de publicacion). Este repo es PUBLICO: solo se sube el post final.
_drafts/
